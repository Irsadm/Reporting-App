{% extends 'templates/default.twig' %}

{% block title %} Group | All Users {% endblock %}

{% block headContent %}
<i style="color:green" class="fa fa-check-square-o">All users of the {{ group }}</i>
{% endblock %}

{% block content %}

<div class="row">
	<div class="col-xs-12">
		<div class="panel panel-default">
			<div class="panel-body">
				{% if login.status == 1 %}
				<form action="{{ path_for('user.group.set') }}" method="POST">
				{% elseif login.status == 2 %}
				<form action="{{ path_for('pic.user.group.set') }}" method="POST">
				{% endif %}
					<table id="example2" class="table table-bordered table-striped table-hover">
						<thead>
							<tr>
								<th>Image</th>
								<th>Name</th>
								<th>Username</th>
								<th>Gender</th>
								<th>Address</th>
								<th>Email</th>
								<th>Phone</th>
								<th>Status</th>
								<th style="width: 40px">Select</th>
							</tr>
						</thead>
						{% for user in members %}
						{% if (user.user_status == 0) %}
						{% set status = 'Member' %}
						{% elseif (user.user_status == 1) %}
						{% set  status = 'PIC' %}
						{% endif %}
						<tr>
							<td><img src="{{ base_url() }}/assets/images/{{ user.image }}" width="100" height="100"></td>
							<td>{{ user.name }}</td>
							<td>{{ user.username }}</td>
							<td>{{ user.gender }}</td>
							<td>{{ user.address }}</td>
							<td>{{ user.email }}</td>
							<td>{{ user.phone }}</td>
							<td>{{ status }}</td>
							<td class="text-center"><input type="checkbox" name="user[]" value="{{ user.id }}"></td>
						</tr>
						{% endfor %}
					</table>
					<hr style="margin-top:10px;margin-bottom:10px;" >
				</div>
				<div class="panel-body" align="right">
					<input type="hidden" name="id" value="{{ group_id }}">
					<button type="submit" name="member" value="1" class="btn btn-success"><i class="fa fa-user-circle-o fa-lg" aria-hidden="true"></i> Set as Member</button>
					<button type="submit" name="pic" value="3" class="btn btn-primary"><i class="fa fa-user-plus fa-lg" aria-hidden="true"></i> Set as PIC</button>
					<button type="submit" name="delete" value="4" class="btn btn-danger"><i class="fa fa-user-times fa-lg" aria-hidden="true"></i> Delete</button>
					<hr style="margin-top:10px;margin-bottom:10px;" >
				</form>
			</div>
		</div>
	</div>
</div>

{% endblock %}
