{% extends 'templates/default.twig' %}
{% block content %}

    <div class="panel-body">
        <div class="box box-info">
          <a href="{{path_for('user.setting')}}"><button type="submit"
              class="btn btn-social-button btn-github pull-right pull-right" style="margin:10px">
              <i class="fa fa-refresh"></i> Kembali</button></a>

        <a href="#"><h4 class="text-center"><i class="fa fa-user-secret">  Ubah &nbsp;Password</i></h4></a>
                <div class="box-header">
        <form action="{{ path_for('user.change.password', {id:login.id} ) }}" method="POST">
         <div class="form-group {{ errors.password ? 'has-error' : '' }}">
                    <label>Password lama:</label>
                    <div class="input-group">
                        <div class="input-group-addon">
                            <i class="fa fa-lock"></i>
                        </div>
                        <input type="password" class="form-control input-medium bfh-password"
                         name="password" value="" placeholder="Password lama">
                    </div>
                    {% if errors.password %}
                        <span class="help-block">{{errors.password | first}}</span>
                    {% endif %}
                </div>

             <div class="form-group {{ errors.new_password ? 'has-error' : '' }}">
                    <label>Password Baru:</label>
                    <div class="input-group">
                        <div class="input-group-addon">
                            <i class="fa fa-lock"></i>
                        </div>
                        <input type="password" class="form-control input-medium bfh-password"
                         name="new_password" value="" placeholder="Password baru">
                    </div>
                    {% if errors.new_password %}
                        <span class="help-block">{{errors.new_password | first}}</span>
                    {% endif %}
                </div>

                 <div class="form-group {{ errors.new_password ? 'has-error' : '' }}">
                    <label>Ulangi Password:</label>
                    <div class="input-group">
                        <div class="input-group-addon">
                            <i class="fa fa-lock"></i>
                        </div>
                        <input type="password" class="form-control input-medium bfh-password"
                         name="retype_password" value="" placeholder="Ulangi password">
                    </div>
                    {% if errors.new_password %}
                        <span class="help-block">{{errors.new_password | first}}</span>
                    {% endif %}
                </div>
                <button type="submit" class="btn btn-default" style="margin:10px">
                    <i class="fa fa-cog"></i> Simpan perubahan</button>
    </form>
        </div>
    </div>
</div>
{% endblock %}
