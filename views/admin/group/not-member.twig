{% extends 'templates/default.twig' %}

{% block title %} Search Users {% endblock %}

{% block headContent %}
	 <i style="color:green" class="fa fa-user-circle-o"> {{ count }} Users found</i>

{% endblock %}

{% block content %}

<div class="row">
	{% for user in users %}
		<div class="col-sm-2 col-md-3">
			<div class="panel box-body">
			<center><img src="{{ base_url() }}/assets/images/{{ user.image }}"
				 height="151" width="151" style="margin-bottom:5px"></center>
			  	<div class="caption">
                    <div class="col-lg-12">
                        <p class="text-center">
							<b>{{user.name}}</b><br><br>
							<i class="fa fa-phone" aria-hidden="true"></i> {{user.phone}}<br>
						</p>
						<br>
                        <center>
							<form action="{{ path_for('pic.member.group.set') }}" method="POST">
							<input type="hidden" name="group_id" value="{{ group_id }}">
							<button type="submit" name="user_id" value="{{ user.id }}" class="btn btn-primary">
								<i class="fa fa-plus-o" aria-hidden="{{ user.id }}"></i>
								Add user to group
								</button>
							</a>
							</form>
                        </center>
                    </div>
			  	</div>
			</div>
	  	</div>
	{% endfor %}
</div>
	{% if users == null %}
	<hr style="margin-top:10px;margin-bottom:10px;" >
	<p align="center" style="color:red">Data not found</p>
	<hr style="margin-top:10px;margin-bottom:10px;" >
	{% endif %}
	<hr style="margin-top:10px;margin-bottom:10px;" >
</form>
{% endblock %}
